import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.time.format.DateTimeFormatter;

import sistema.*;

/*
- Incluir la fecha de nacimiento en la clase Usuario - Gerente, CLiente, Trabajador
- Implementar los metodos de checar entrada y salifa para gerentr y trabajador
_ Investigar que es una libreria (externa), para que se usan, ejemplos de ellas, minimo 2 (programadas) y como se instalan
*/

public class Main {
    public static void main(String[] args) {
        Menu.ejecutarMenu();

        /*
         LocalDate fecha = LocalDate.of(2000, 10, 5);
        System.out.println(fecha);
         * 
         LocalTime hora = LocalTime.now();
        System.out.println(hora);
         * 
         * 
         * 
        */

        /*
        YYYY - Year
        MM - Month
        dd - Day
        HH - Hour
        mm - Minutes
        ss - Seconds
         */


        /*

        LocalDateTime fechaCompleta = LocalDateTime.now();
        System.out.println(fechaCompleta);


        DateTimeFormatter pattern = DateTimeFormatter.ofPattern("dd/MM/YYYY HH:mm");

        String fechaFormateada = fechaCompleta.format(pattern);

        System.out.println(fechaFormateada);

         */

        

        
    }
}